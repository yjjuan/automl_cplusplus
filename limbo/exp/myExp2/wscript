from waflib.Configure import conf

def options(opt):
    pass


def build(bld):
    bld(features='cxx cxxprogram',
        source='main.cpp',
        includes='. ../../src',
        target='myExp2',
        uselib='BOOST EIGEN TBB LIBCMAES NLOPT',
        use='limbo')